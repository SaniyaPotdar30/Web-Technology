/* DOM - document, object, model
- it is a bridge between html and javascript
- programming interface for html
- we have learnt core js but it cannot make changes in web pages that is why we
use DOM
- when we load browser, html changes to object. That obj structure is called 
as DOM
- so DOM is used to make changes in the contents of js

document.getElementbyId(id="title").id
.class
.className
.getAttribute
.setAttribute

inner html
inner textcontent

query selector - it takes only one value from list
query selector all - takes all value

activity- create simple webpage
        - create project using js and dom (2)
            -design 
*/